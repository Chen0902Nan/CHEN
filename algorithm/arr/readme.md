- 数据结构要搞哪些？

  ## 线性数据结构

  - 数组 常用、好用、连续内存
  - 栈 先进后出 FILO(first in last out)
  - 队列 先进先出 FIFO(first in first out)
  - 链表 离散

  ## 非线性数据结构

  - 树(二叉树)

## 数组 **开箱即用**的数据结构

连续，O(1)的复杂度

- 数组的创建
  - 了解了数组的每一项
    const arr = [1, 2, 3, 4, 5, 6]
  - 不知道内部的元素情况？ 初始化一个数组
    const arr = new Arry(10).fill(0)
    - 要多了浪费
    - 要少了怎么办？
      二次申请内存空间 (10) 开销比较大
      JS 支持动态扩容
      搬家
      考虑到动态性的时候 数组是比链表差的
      少数数据的情况下，数组表现更加优秀，链表节点的实例化多个指针

## 遍历数组

arr[index] O(1)

- for 循环
