# es6+

- 2015 年作为分水岭 之前 js
  有好的一方面（单线程，简单），
  也有坏的一方面（变量提升，早期 class 关键字都没）
- js 向企业级开发语言靠拢，别的语言有的要有，适合大型项目开发

## 解构

- 一次性声明多个变量
- 解构了数组，甚至还可以解构嵌套数组
- 解构对象
- 左右两边要求一致

## reset 运算符 ...

## 对象的属性简写

## 模版字符串

- ${} 差值表达式 运行 返回值

## for of 循环

简单循环 语义更好 可读性好 -> 相对于 计数循环

## bigint 大数

- 一种新的简单数据类型
  在数字末尾添加一个 n

## \*\* 指数运算符 es7 2016

## 函数的默认参数

## 一次性接收多个不固定参数

function foo(...args){}
