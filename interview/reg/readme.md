# 写一个函数，可以转化下滑线命名到驼峰命名

acb-cdf abcCdf -qwe-try qweTry

## 正则

- 查找 for
- 正则可以用一套规则在字符串中匹配或查找

## 判断是不是手机号

- 一个一个字符的匹配
  1就是具体匹配
  \d表示数字
  \w 字母
  {10} 长度 10个
  ^字符的开始
  $字符的结束
  \s表示空格 +贪婪匹配
  [,\s] 表示一个字符组，匹配,和空格

- test方法 true|false 是否匹配正则
- str.match 匹配的结果
