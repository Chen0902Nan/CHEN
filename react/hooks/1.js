function add(nums) {
  nums.push();
  return nums.reduce((pre, cur) => pre + cur, 0);
}
const nums = [1, 2];
add(nums);
console.log(nums.length);
const add = function (x, y) {
  fetch; //不确定的
  return x + y;
};
