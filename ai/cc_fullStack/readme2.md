# AI 全链路开发流程

- AI Agent 开发平台
- 角色转移

成为一名架构师，当拿到一个复杂系统的需求，如何拆解成为Claude Code能准确执行的任务链，一个人从零交付出来

- 首先需要干嘛？
  架构师？不只是写代码的，10%写代码
  程序员(n个) 产品经理，运维 设计 测试 虚拟数字员工
  - 不要急于写代码
    古法编程 效率不高
  - 不要急于让cc干活，没想清楚的话，ai会帮倒忙
    帮我设计一个Agent 开发平台的后端架构，支持多模型、多Agent、工具调用
    不够明确(上下文，猜)，大模型好的标准(CLAUDE.md)、边界
    llm会很快给我们一个方案，微服务架构 技术选项复杂，甚至专业到架构画图
    文件结构目录
    vibe coding 优势不是代码开发效率，是转变思维的我们
    vibe coding 区别不再是llm，而是有想法，有方法论，harness的人
    不要完全相信llm返回

## 角色的转变

从写代码到做决策
问题不再是工具，要想清楚再让他干活

1. 有没有定义清除产品的边界，什么做，什么不做，llm不用猜
2. 没有明确的架构约束，这是一个人一周要交付的单人项目，不是一个要3年的团队项目
3. 没有给任何的规范 命名规范(驼峰式)，接口怎么设计(restful，报错怎么做) 返回的格式 data:{} code message
   不是不能做好 而是我们要告诉它好的标准

用Claude Code做项目，瓶颈不再是AI能力，而在你的思考质量
架构师和工程团队的关系
Claude Code是你的工程团队，而我们必须成为架构师 - 决定做什么
用什么方法做

## 方法论 不只是写代码，而是拆解问题，咨询，决策

- 执行模式，任务拆分清除了让cc做，我们验收
- plan模式，你还没有拆分清除 让 cc帮我们梳理，取舍再让它执行

## 实战

结果的到 CLAUDE.md

1. 产品定义 动第一行代码之前我们想清楚做什么？
2. 帮我梳理 Dify（https://dify.ai）这个产品的核心功能模块，按类别分组，每个模块用一两句话说明它做什么。

AI的信息不要直接信，我们要验证(读文档，看github,论坛聊聊 几小时 )
最终梳理出dify的功能全景

3. 功能取舍 从 dify到 Cify简版
   我要基于 Dify 做一个简化版的 AI Agent 平台，叫 Cify。约束条件：一个人开发，面向团队内部 20-50 人使用，本地部署。请从刚才梳理的功能列表中，帮我判断哪些是必须做的核心功能，哪些可以砍掉，给出每个的理由。

三问裁剪法

1. 没有它产品还能成立吗？
   模型管理、Agent配置、对话引擎、工具集成、管理控制台、砍掉任何一个都不成立
   知识库、工作流 砍掉成立，但价值大打折扣，降级做
   可视化拖拽、多租户、插件市场、计费 砍掉不影响核心链路
2. 做到什么程度够用
   支持多少台服务器并发？
   知识库 文本
   chatbot 流式
3. 能不能一句话讲清楚？
   Cify：一个可本地部署的 AI Agent 开发平台，支持多模型管理、Agent 配置、知识库 RAG、简版工作流、对话交互和 MCP 工具接入，面向团队内部小规模使用。

- 技术选择
  Hify 是一个 AI Agent 开发平台，一个人开发，本地部署，目标 20-50 人使用。帮我对比以下技术方案的优劣：1) Spring Boot + Vue 2) Go + React 3) Python FastAPI + React。重点考虑开发效率、生态成熟度、AI 领域 SDK 支持、运维复杂度。

```
## 项目概述

Hify 是一个简版的 AI Agent 开发平台（参考 Dify），可本地部署，
面向团队内部小规模使用（20-50 人同时在线）。

### 做什么

- 多模型提供商管理（OpenAI、Claude、Gemini、Ollama）
- Agent 创建与配置（选模型、绑工具、设系统提示词）
- 对话引擎（流式响应、多轮对话、上下文管理）
- 知识库 + RAG（一期只支持 TXT 文档，固定长度分块）
- 简版工作流（JSON 配置，线性 + 条件分支，不做可视化拖拽）
- MCP 工具接入（Agent 可通过 MCP 协议调用外部工具）
- 管理控制台（模型管理、Agent 配置、对话界面）

### 不做什么

- 不做可视化工作流拖拽编排
- 不做多租户 / 权限体系
- 不做插件市场、计费系统
- 不做文本生成应用、WebApp 发布、嵌入组件
- 不做标注与微调

### 技术栈

后端：Spring Boot 3.x + MyBatis-Plus + MySQL 8.x + Redis 7.x
前端：Vue 3 + TypeScript + Vite + Element Plus
容器化：Docker + Docker Compose

### 部署与运维预期

- Docker Compose 本地一键部署，JVM 内存设上限（-Xmx512m）
- 目标：20-50 人同时在线，峰值 3-5 QPS，瓶颈在 LLM 长连接
- 缓存：Redis Cache-Aside（配置信息 + 会话上下文）
- 监控：起步 Actuator + 日志，后期 Prometheus + Grafana
```

我们用了 咨询模式
在动手之前，我是怎么让 Claude Code 帮我梳理 Dify 的功能全景、怎么用三问裁剪法确定 Hify 的边界、怎么让它给架构方案然后我来拍板的

## AI 是放大器，不是发动机

你有方向(正确、清晰、可为、标准) 放大十倍
没有方向它放大还是0

## 我们和Claude Code 是什么关系

架构师，所有事情的负责人
动手之前，比怎么做更重要的是想清楚产品需求、架构设计、测试、产品经理、运维，这些事情谁来做。

claude code 瓶颈不再是AI，在我们

## 三方面

- 同样的工具不同人用的效果差那么多？
  CLAUDE.md(活的，会成长的) SDD harnes
  花时间在思考上 定义产品边界，架构约束，技术规范
  思考的质量 + SDD = llm输出质量
- 我们和Claude Code 各自该干什么？
  我们做决策 CC做执行
- 拿到它的输出之后怎么去验收?
  ai 生成的每一行代码 我们要了解 不需要程序员(不可能)
  代码风格 代码正确性 代码的边界

架构师每天干什么?

1. 他不写具体的业务代码，但他决定了系统长什么样。
2. 他定方向：做什么、不做什么。
3. 他做取舍：遇到矛盾选 A 还是选 B。
4. 他立标准：代码怎么写、接口怎么定、出了错怎么处理。
5. 他验收成果：团队交付的东西是不是他要的。

CC做什么
工程团队在干什么？

在按标准高效执行。

你给 Claude Code 下达的每一条指令，本质上就是架构师在给团队派任务
