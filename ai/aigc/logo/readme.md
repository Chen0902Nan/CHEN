# coze logo 生成

- 选择合适的模型
  dalle-e-3
- prompt
- bootstrap 样式库
  .container PC layout 中间宽度,左右 margin：auto
  .form-group

## HTML5 Form 表单

- 给必填字段 required 属性
- placeholder="" 属性 提示用户输入，把用户当小白
- label for + input#id 为盲人而来（读屏器）-> 无障碍访问
- 点击按钮，表单的提交
- form 默认提交到 action 为空就是当前页面
  - event.preventDefault() 阻止表单的默认提交

## apifox

## AIGC LLM 的本质

- node openai completions
- llm 发起的 **api** 调用 前端界面 api LLM
  - POST 请求
    apiKey 加密
  - GET 请求
    明文
