# 手写 keep alive

- 缓存组件 keep alive
  ? Map es6 新增的数据结构
  key 超越string object key 可以
  json 区别？ key:value
- 切换显示

- 考察我们手写组件 KeepAlive
  - children keepalive
    Map
    所有组件都显示
  - props?
    active display:block
    display:none

- Object.entries
  把对象键值对拆成数组，key是数组第一项，value 是数组第二项
  二维数组 [[key,value],[key,value]]
