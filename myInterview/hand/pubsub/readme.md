# 订阅发布者模式

- 订阅发布者模式是一种面向对象中的设计模式。定义了一种一对多的依赖关系，让多个订阅者通过事件中心(EventEmitter)来监听发布者的消息，实现对象之间的解耦通信。
  比如抖音、微信公众号、
- 手写实现
  采用class类定义一个EventEmitter
  constructor 定义一个对象存储事件和订阅者 key为事件名，value为回调函数数组->订阅者
  定义 on、off、emit、once函数
  once就是先加入订阅，但是执行一次就卸载，要结合off和on，用onceFn包裹，保存函数引用，执行完后off卸载实际函数
