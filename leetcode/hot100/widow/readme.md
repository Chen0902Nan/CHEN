## 438——找到字符串中所有字母异位词

这题就是通过开length=26的数组来存储字母，通过p[i].charCodeAt()-'a'.charCodeAt()来判断当前的字符串是什么，然后通过.toString判断两个数组是否全等，即可AC

## 3——无重复字符的最长子串

双指针结合Map即可AC，通过双指针维护一个子串，每个不重复的map都存入，当某个字符的值大于1，就收缩左指针直到大于一的字母不重复了 
